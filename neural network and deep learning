deep learning and neural netowrks

what is deep learning and neural networks?
a single function act as a neuron and multiple fuctions act as neural network
on given input  neural network create input features and give output.

##supervised learning?
 structured data and unstructured data
 size numbers and audio image 
 
 #binary classificatio?
 it is a cat or not. it is yes or no ,1 or 0
 we have an image and each pixel is a value of 3 color code(rbg from 0 to255)
 we have to convert the values into  input feature vector,
 
 #logistic regression?
 logistic regression cost function?
 
 
 
